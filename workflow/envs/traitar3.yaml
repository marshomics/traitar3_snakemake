name: traitar3-smk
channels:
  - conda-forge
  - bioconda
dependencies:
  - python >=3.9
  - hmmer >=3.3,<3.5        # HMMER3; reads Pfam 27.0 HMMs, supports --cut_ga
  - numpy >=1.21
  - pandas >=1.3
  - curl
